# Binaries (root-level build output only)
/docker-hash
*.exe
*.test

# Build output
/dist/

# Go vendor directory
/vendor/

# Defensive: markdown tooling is installed under .github/markdownlint for
# CI/hooks, and contributors may also run npm commands in subdirectories.
# Node modules should never be committed in this Go repo.
node_modules/
/package.json
/package-lock.json

# Local machine overrides for mise
.mise.local.toml
